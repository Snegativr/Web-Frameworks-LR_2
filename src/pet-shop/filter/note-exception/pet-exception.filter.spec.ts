import { PetsExceptionFilter } from './pet-exception.filter';

describe('PetsExceptionFilter', () => {
  it('should be defined', () => {
    expect(new PetsExceptionFilter()).toBeDefined();
  });
});
